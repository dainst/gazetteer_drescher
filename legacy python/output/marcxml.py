import logging

logger = logging.getLogger(__name__)
logger.setLevel(logging.INFO)

def writeBatchToFile(batch, requestInfo):
  logger.error("MARCXML not implemented yet, script added only for testing.")
